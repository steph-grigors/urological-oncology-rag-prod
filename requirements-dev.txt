# Development and CI dependencies.
#
# Installing this file also installs everything in requirements.txt, so CI and
# local development need only:  pip install -r requirements-dev.txt
#
# These are NOT installed into the runtime Docker image (docker/Dockerfile
# installs requirements.txt only), so nothing here reaches production.

-r requirements.txt

pytest>=8.0.0            # test runner — the CI jobs invoke `pytest` directly
pytest-mock>=3.14.0      # provides the `mocker` fixture (tests/unit/test_confidence_gating.py)
httpx>=0.27.0            # required by fastapi.testclient.TestClient (integration tests)
coverage>=7.0.0          # local coverage runs: coverage run -m pytest && coverage report
