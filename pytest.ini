[pytest]
markers =
    integration: marks tests as integration tests (in-memory Qdrant; no live APIs required)
    eval: marks tests as evaluation/quality-gate tests (run with mock pipeline; no live APIs required)
